# ------------------------------
# 1. Basic Settings & Performance Fixes
# ------------------------------
set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm*:RGB"
set -s escape-time 0
setw -g mode-keys vi
set -g history-limit 10000
set -g base-index 1
setw -g pane-base-index 1
set -g allow-rename on
set -g renumber-windows on
set -g mouse on
set -g set-titles on
setw -g monitor-activity on
set -g bell-action any
set -g visual-bell off
set -g visual-activity off
set -g focus-events on
set -g extended-keys on
setw -g aggressive-resize on
set -g detach-on-destroy off

# ------------------------------
# 2. Prefix Configuration
# ------------------------------
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# ------------------------------
# 3. Plugins (Define & Initialize)
# ------------------------------
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'sainnhe/tmux-fzf'

set -g @plugin 'egel/tmux-gruvbox'
set -g @tmux-gruvbox 'dark'
set -g @tmux-gruvbox-left-status-a ' '
set -g @tmux-gruvbox-right-status-z ' '

set -g @continuum-restore 'on'

# Initialize TPM (must happen before custom key overrides)
run '~/.tmux/plugins/tpm/tpm'

# ------------------------------
# 4. Custom Keybindings (The "Winners")
# ------------------------------

# Reload config
unbind %
bind % source-file ~/.config/tmux/tmux.conf \; display-message 'Config reloaded'

# Lazygit
# sudo apt install lazygit
bind-key G new-window -n lazygit -c "#{pane_current_path}" "lazygit"

# Lazydocker
# curl https://raw.githubusercontent.com/jesseduffield/lazydocker/master/scripts/install_update_linux.sh | bash
bind-key D new-window -n lazydocker -c "#{pane_current_path}" "lazydocker"

# Kill pane & window safety
unbind x
bind x confirm-before -p "Kill pane? (y/n)" kill-pane
bind X confirm-before -p "Kill entire window? (y/n)" kill-window

# Splits
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# New window logic
bind c new-window -a -c "#{pane_current_path}"

# Copy mode (Fixed Space conflict)
unbind Space
bind Space copy-mode
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi V send-keys -X select-line
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# Window navigation
bind-key -n C-S-Left previous-window
bind-key -n C-S-Right next-window

unbind s
bind s display-popup -E "
  tmux list-sessions -F '#{?session_attached,,}#{session_name}' |
  grep -v '^$' |
  fzf --reverse --header '  SESSIONS' --color='header:#b8bb26' |
  xargs tmux switch-client -t
"

unbind w
bind w display-popup -E "
  tmux list-windows -F '#{window_index} #{window_name}' |
  grep -v '^$' |
  fzf --reverse --header '󱂬  WINDOWS' --color='header:#b8bb26'|
  cut -d ' ' -f 1 |
  xargs tmux select-window -t
"

# # =============================================================================
# # 5. TMUX COLOR VARIABLES (Gruvbox Soft Palette)
# # =============================================================================
# BG="#32302f"          # Gruvbox Dark Soft Background
# FG="#f2e5bc"          # Gruvbox Light Soft Foreground (Warm cream)
# DARK_GRAY="#3c3836"   # Line highlights / Panels (bg1)
# LIGHT_GRAY="#504945"  # Comments / Invisible characters (bg2)
# SELECTION="#665c54"   # Visual mode selection (bg3)
# RED="#fb4934"         # Bright Red (Errors / Git deletions)
# GREEN="#b8bb26"       # Bright Green (Success / Git additions)
# YELLOW="#fabd2f"      # Bright Yellow (Warnings)
# BLUE="#83a598"        # Bright Blue (Mode indicators)
# MAGENTA="#d3869b"     # Bright Purple (Keywords)
# CYAN="#8ec07c"        # Bright Aqua (Operators / Info)
#
# # =============================================================================
# # 5. STATUS BAR LAYOUT CONFIGURATION
# # =============================================================================
# set -g status-position bottom
# set -g status-justify left
# set -g status-interval 5
# set -g status-style "bg=${BG},fg=${FG}"
#
# # Left Side (Shows Session Name)
# set -g status-left-length 30
# set -g status-left "#[fg=${BG},bg=${GREEN},bold]  #S #[fg=${GREEN},bg=${BG},none] "
#
# # Window List Style
# set -g window-status-separator ""
# set -g window-status-format "#[fg=${FG},bg=${BG}] #I:#W "
# set -g window-status-current-format "#[fg=${BG},bg=${GREEN}]#[fg=${BG},bg=${GREEN},bold] #I:#W #[fg=${GREEN},bg=${BG}]"
#
# # Right Side (Shows Clock & Date)
# set -g status-right-length 60
# set -g status-right "#{prefix_highlight}#[fg=${LIGHT_GRAY},bg=${BG}]#[fg=${FG},bg=${LIGHT_GRAY}] %H:%M #[fg=${GREEN},bg=${LIGHT_GRAY}]#[fg=${BG},bg=${GREEN},bold] %d-%b-%y "
#
# # Prefix Highlight Styling (Leveraging our variables)
# set -g @prefix_highlight_fg "${BG}"
# set -g @prefix_highlight_bg "${GREEN}"
# set -g @prefix_highlight_show_copy_mode 'on'
# set -g @prefix_highlight_copy_mode_attr "fg=${BG},bg=${FG},bold"
