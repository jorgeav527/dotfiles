-- https://github.com/AndrewRadev/splitjoin.vim
return {
    'AndrewRadev/splitjoin.vim',
    event = 'VeryLazy',

    config = function()
        -- You *can* customize mappings if desired here,
        -- but defaults are usually fine.
    end,
}
